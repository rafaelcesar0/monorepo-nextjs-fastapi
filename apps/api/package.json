{
	"name": "api",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"sync": "uv sync",
		"dev": "uv sync && uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
		"start": "uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers ${UVICORN_WORKERS:-2}",
		"build": "echo 'No build step for Python API'",
		"lint": "echo 'Python lint placeholder'",
		"test": "echo 'Python test placeholder'"
	}
}
